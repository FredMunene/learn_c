#include <stdio.h>

int main(){
    // % modulus
    // ++ increment
    // -- decrement
    int y = 5;
    int x = 11;
    // x++;
    // float z = x / (float) y;
    // printf("%f\n", z);
    // x=x+2; 
    x+=2; // Augmented assignment operator: applies for all operators
    printf("%d\n", x);



    return 0;
}