#include <stdio.h>

int main()
{
    //constants - fixed values that cannot be altered during execution. READ ONLY VARIABLE.

    const float PIE = 3.14159; // constants should be upper caps

    printf("value of pi is always %f \n", PIE);

return 0;

}